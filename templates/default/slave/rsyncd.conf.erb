## Global Settings
uid = root
gid = root
dont compress = *.gz *.tgz *.zip *.pdf *.sit *.sitx *.lzh *.bz2 *.jpg *.jpeg *.gif *.png
log file = /var/log/rsyncd.log
pid file = /var/run/rsyncd.pid

[public]
    path = <%= node[:lsyncd][:sync_path] %>
    hosts allow = <%= node[:lsyncd][:master_host] %>
    read only = no

